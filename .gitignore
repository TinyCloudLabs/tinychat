node_modules/
dist/
.env
.env.local
.env.prod
*.tsbuildinfo
.DS_Store
*.pem
.turbo/
.claude/
.auth/
test-results/
playwright-report/
.playwright-mcp/
blob-report/
traces/
videos/
screenshots/
test/.passkey.json
test/.last-run.log

# Connector verdict probes are throwaways; the checked-in fixtures/*_VERDICT.json are NOT.
# The exploit narrative (resource strings + node responses) stays out of git while unpatched.
test/connectors/probe-*.ts
test/connectors/*.run.log

# Real-auth browser profiles and artifacts
.playwright/

# Smithers workflow audit artifacts (transient — one dir per feature workflow)
.*-audit/

# Smithers local run state
.smithers/executions/
.smithers/logs/
smithers.db
smithers.db-shm
smithers.db-wal
